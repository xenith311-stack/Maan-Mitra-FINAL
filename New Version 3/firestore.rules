rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // LAUNCH-READY RULES - Simple and effective
    // All authenticated users can read/write their own data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 